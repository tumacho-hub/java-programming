package day_44_custom_classes;

public class App { // class always with capital letter
    // we can create objects from the class only !
    // app class is used as temple for app objects!
    // we are describing app- properties of the app behaviour.
    // objects will be able to use them.

     String name; //data
     double version; // data

     public void open(){ // behaviour
         System.out.println("opening " + name + "app- version = " + version);// behaviour

     }



}
